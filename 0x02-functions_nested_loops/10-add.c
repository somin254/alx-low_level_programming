#include "main.h"
#include <stdio.h>
/**
 * add - adds two integers and returns the result
 * @a: first integer
 * @b: second integer
 *
 * Return: sum of a and b
 */
int add(int a, int b)
{
return (a + b);
}
