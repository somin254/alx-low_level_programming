#include"main.h"
#include<stdio.h>
/**
 * *_strcat -  function appends the src string to the dest string
 * Return: dest
 */
int main(void)
{
char *_strcat(char *dest, char *src)
{
int r = 0, j = 0;
while (dest[r] != '\0')
i++;
while (src[j] != '\0')
{
dest[r] = src[j];
r++;
j++;
}
dest[r] = '\0';
}
